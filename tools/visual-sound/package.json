{
  "name": "visual-sound",
  "version": "1.0.0",
  "description": "Tool for audio signal preview",
  "main": "index.js",
  "scripts": {
    "mic": "browserify points/visual-sound-mic.js -p esmify > builds/visual-sound-mic.js",
    "file": "browserify points/visual-sound-file.js -p esmify > builds/visual-sound-file.js",
    "all": "npm run mic && npm run file",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ai"
  ],
  "author": "Nikola Lukic",
  "license": "ISC",
  "dependencies": {
    "nidza": "^1.2.18",
    "browser-resolve": "^2.0.0"
  },
  "devDependencies": {
    "esmify": "^2.1.1"
  }
}
